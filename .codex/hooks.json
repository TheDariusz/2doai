{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; case \"$f\" in *frontend/*.ts|*frontend/*.tsx) dir=\"${f%/frontend/*}/frontend\"; (cd \"$dir\" && npx eslint --fix \"$f\"); ;; esac; } 2>/dev/null || true",
            "statusMessage": "Linting frontend file..."
          }
        ]
      }
    ]
  }
}
